# 只装「跑单测与 lint 需要的」依赖 —— CI 用，刻意不含 torch / sentence-transformers。
#
# 为什么可以省：单元测试一律用零依赖的 HashingEmbedder（EMBEDDER=hash）与假 LLM，
# 管线逻辑（分块、幂等、阈值、SSE、兜底）与模型无关。为了这点把 400MB+ 的
# torch 拖进每次 CI，只会让流水线慢三分钟、并且和「测试到底在测什么」无关。
# 真机要跑 bge 语义检索时，仍按 requirements.txt 装。

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic-settings>=2.4
httpx>=0.27
pypdf>=5.0
numpy>=1.26
chromadb>=0.5.5
fpdf2>=2.7

pytest>=8.0
pytest-asyncio>=0.24
ruff>=0.6
